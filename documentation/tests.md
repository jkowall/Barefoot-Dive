# Test coverage and verification

## Commands

| Command | Current purpose |
| --- | --- |
| `npm run lint` | ESLint source/config check |
| `npm run test` | All Vitest unit suites |
| `npm run build` | TypeScript no-emit check and Vite production build |
| `npm run check` | lint, unit tests, and build |
| `npm run verify:deco` | `src/engine` suites |
| `npm run verify:gas` | gas and calculator suites |
| `npm run verify:exposure` | domain validation and tissue suites |
| `npm run verify:cave` | cave suites |
| `npm run test:ui` / `npm run test:visual` | Playwright smoke/visual browser gates. Each run builds, then serves its own preview on a free port, preferring one derived from the checkout's path and moving on when another server answers there or another run has reserved it; `PLAYWRIGHT_PORT` pins a port from 1024 to 65535, and `PLAYWRIGHT_REUSE_SERVER=1` reuses a preview on the preferred port without building |
| `npm run build:mobile` | web build plus Capacitor iOS/Android sync |
| `npm audit --omit=optional` | registry advisory check for the installed dependency tree |
| `cd android && ./gradlew assembleDebug` | unsigned Android debug build |
| `xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Debug -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO build` | unsigned iOS simulator build |

## Current suites

Vitest covers domain units/validation, compile-time absolute/gauge/delta pressure separation, planner and tissue behavior, published reference-comparison envelopes, source-backed calculator fixtures, calculator/planner parity, gas ledger/reserves, characterization of the RMV phase boundary (the open-circuit climb to the first stop and a no-stop ascent charged at the deco RMV, and bailout travel legs at the bailout RMV, both synthetic and through the planner), the opt-in first-stop boundary (climb and no-stop ascent at the bottom RMV, the golden 45 m trimix plan moving only its 51.9 L climb volume with an identical schedule, a gas switched to before the first stop, a switch made on arrival at the first stop counted as the stop, identical cylinder reserves under all five reserve policies, gas-only thirds and sixths reserves rising by half and twice the added use, reserve crossings moving only earlier, open-water event plans, unchanged CCR bailout ledgers, and validation of the value, mode, and environment), saved-plan recalculation keeping each plan's deco RMV rule, a pinned digest for the 0.5.0 default OC draft, cave routes/scenarios, app planning resolution (including exclusion of switched-off deco and bailout gases, gas-only resolution, the emitted CCR setpoint and dil-out fields, and the first-stop field emitted only for open-water OC drafts with the switch on, which adds exactly the default draft's 42.86 L climb volume), CCR low/high setpoint descent against a hand-computed Schreiner value, leave-the-depth switch-down at grid, off-grid, and sub-last-stop depths, switch-down deepening to where the high setpoint is achievable (including the no-stop limit), arrival-ceiling invariants after deep switch-downs, the engine 0.2.1 arrival re-check (the 60 m Tx12/60 bailout first stop, a stop held until the next leg's arrival clears, a first stop kept at the current depth when no shallower grid depth clears, a pinned open-circuit schedule that surfaces sooner because the deeper first stop anchors the gradient-factor line, ceiling invariants for every ascent, bailout, and stop segment across open-circuit nitrogen-to-Tx30/30 and legacy CCR/bailout sweeps, open-circuit helium-bottom-gas sweeps that also passed before 0.2.1 and serve as regression guards, leg-only invariants for air-loop bailouts at 75–100 m with the pre-existing 6 m loop-plan failures asserted explicitly, and a characterization test of the remaining stop-hold limitation), stops moved to a richer gas instead of a 48-hour stop, bailout-failure propagation, setpoint and dil-out validation boundaries, bailout eligibility, surface, and coverage-gap checks, dil-out selection and diluent deductions, hypoxic-leg waypoints and the fatal hypoxic-segment guard, gas-only reserve inversions against cylinder mode, the gas-only zero-reserve warning and cylinder-plan max PPO₂ rejection, Tank Bank mix carry-over into gas-only drafts, imperial switch-depth snapping, cave low-setpoint splits, dil-out loop failure, dil-out turn floor, pre-bailout scaling, and gas order, saved-plan recalculation of a 0.1.0 CCR record (legacy breathing, lineage, untouched parent), 0.4.0 field round-trips and malformed stored setpoints, a legacy golden fixture guard (seven inputs captured from engine 0.1.0, including a legacy CCR-to-CCR event plan under the Shearwater preset; a guard, not a proof over all inputs), profile-timeline geometry, presentation-only runtime-schedule row grouping, storage schema/snapshot/revision behavior, Tank Bank per-record quarantine (a current valid envelope, one invalid record in a current envelope with no write on read, byte-exact carry-through of a quarantined record's stored text in the same relative order across create, edit, archive, restore, duplicate, and delete, including text a JSON round trip would alter (`1e400`, an integer beyond 2^53, duplicate keys, escapes, whitespace) and a `records` member located the way `JSON.parse` resolves escaped and duplicate keys, quarantine positions reported from the written bank, refusal to read, mutate, or delete a quarantined id, refusal to write a record the read would quarantine or a quarantined record whose stored text is unmatched, migration of a fully valid legacy bare array, whole-read rejection without any write for a legacy bare array with an invalid record, an unsupported or missing schema version, a non-list container, and a primitive value, and field-level validation paths including nested gas fields), and unchanged all-or-nothing Saved Plans reads (one invalid record fails the read and every read and mutation method is refused without a write; a legacy bare array still reads). Cave regressions include a stage-only route with zero back-gas use, finite input rejection, CCR bailout-cylinder limits, and a bound where longer exposure creates new decompression before gas is exhausted. Playwright covers safety-gate persistence, the default-on Bottom RMV until first stop switch (recalculation, the Review diagnostic and note, and its absence for CCR and Cave), the CCR low-to-high setpoint scrubber, the required dil-out pre-bailout field and diluent bailout ledger, the gas-only ledger and fixed-reserve fix-up, accessible control naming/focus, primary workflows, footer versions/project links and mobile navigation clearance, interactive profile scrubbing, completion-border motion, successful-calculation and newly added editor scrolling, responsive result-metric containment, reduced-motion fallback, offline reload, stored cave-layer safety diagnostics, the Tank Bank quarantine notice (the valid cylinder still listed and offered as a Plan cylinder source, a search with no match reported as such rather than as an empty bank, and the quarantined record's exact stored text kept after a UI edit), the unreadable Tank Bank state (no empty-bank message, Add cylinder disabled, stored data untouched), and committed phone/tablet/desktop visual snapshots (regenerated for the 0.3.0 instrument-dark visual system, and again in 0.5.2 for whole-foot depths, folded stop rows, and SAC/RMV labels, after inspecting every phone, tablet, and desktop image). The exact pass baseline is the result of the commands above in the current checkout; this document does not convert a green test run into field validation or cave-procedure approval.

One Tank Bank cylinder selected for several gases has its own coverage. Vitest (`src/app/planning.test.ts`) pins the unchanged behavior underneath: both gases still resolve to the record's gas and one cylinder, and the planner still rejects that input with the bare `GAS_ID_DUPLICATE` message. It checks that `sharedTankSourceDiagnostics` returns one `TANK_SOURCE_SHARED` error per shared record, naming the gases in plan order and the record, for two and three gases, two shared records, a CCR diluent and bailout gas, and Cave's wording; that `selectedTankSources` lists each loaded record with the active gases that select it; the source-control option label and the per-gas error text; that excluded deco gases, a travel gas that is off, the other breathing mode's gases, gas-only plans (but not Cave), and archived, deleted, quarantined, or unreadable records are never reported as shared; and that each way of separating the gases calculates. `src/app/caveRoute.test.ts` checks that Cave's `ROUTE_CYLINDER_SHARED` errors name the gases and the record exactly as this check does in Cave. Playwright covers the path to the state in Plan: a record the oxygen gas uses is named and disabled in the bottom gas's source control, becomes available when the oxygen gas is switched off, and is shared once it is switched back on. It then checks the "Cylinder shared" status, the named error, no duplicate-identifier text, both source controls and their errors as accessible descriptions, no automatic recalculation, the earlier result matching again once the oxygen gas is switched off, and the same disabled option in Cave. The visual suite captures the shared-cylinder gas editor on phone, tablet, and desktop.

Tank Bank source resolution has its own coverage. Vitest (`src/app/planning.test.ts`) checks that an archived, deleted, quarantined, or unreadable selected record yields no input, one unavailable source with its reason and stored name, and a `TANK_SOURCE_UNAVAILABLE` diagnostic. It also checks that the listed ad hoc values are exactly what a detach calculates, never the record's; that re-selecting or restoring resolves again; that a loaded record wins over a quarantined entry with the same id; that Cave, deco, and CCR bailout gases are reported the same way; that excluded, inactive, and gas-only gases are ignored; and the Tank Bank snapshot adapter, including quarantined ids and names taken from `STORAGE_RECORD_QUARANTINED` read diagnostics. `src/app/workspaceStatus.test.ts` pins the shared Plan and Cave status precedence and checks that a calculation superseded by an unavailable source stays superseded when the source returns unchanged, until an explicit recalculation. Playwright covers an archived source in Plan (reason, exact detach values in PSI and ft³, no automatic recalculation, Gas only off, focus kept on the source control after detaching, explicit update), a deleted source resolved by choosing another cylinder, a quarantined source (a stored record given a text `gas.oxygen` after it was selected) resolved by choosing another cylinder with the stored Tank Bank text unchanged, an unreadable Tank Bank (unsupported schema version) that stays unmodified, an archived source in Cave, and, in both Plan and Cave, an earlier result that stays hidden behind an explicit Update after the Tank Bank becomes unreadable and then returns byte-identical. The visual suite captures the unavailable-source gas editor on phone, tablet, and desktop.

Cave route access has its own coverage. Vitest (`src/app/caveRoute.test.ts`) checks the gas-to-cylinder pairing against independently computed cylinder ids for 700 combinations of breathing mode, travel and include switches, and ad hoc, loaded, archived, or deleted sources. It checks that a leg's recorded access and stage follow their gas through a Tank Bank cylinder, another cylinder, an archived source, its detach, and its restore; that the cave plan then breathes the detached bottom gas on the leg; that an unedited leg lists every plan cylinder in plan order and re-ticking restores the identical cave input; that a leg's first access edit records only the gases then taking part, so a gas switched back on, a travel gas turned on, a newly added gas, and the other breathing mode's gases start inaccessible (the cave layer then rejects a CCR leg without its diluent); that a gas switched off after the edit leaves no stale cylinder and the plan still calculates; that a stage whose gas is switched off is left out and rejected by the cave layer; and that one Tank Bank cylinder selected for two or three gases builds no route, returns `ROUTE_CYLINDER_SHARED` naming the gases, and leaves every gas's access and stage untouched until the gases are separated. Playwright covers the reported case (a leg edited while the bottom gas used a Tank Bank cylinder keeps that gas accessible after "Ad hoc plan cylinder", and the cave plan calculates), a dropped stage that follows its gas to a Tank Bank cylinder and back, and the "Cylinder shared" state, reached by switching the oxygen gas back on after the bottom gas took its record (named gases, a locked mixed checkbox, no automatic recalculation, the earlier result matching again once the oxygen gas is switched off, and each gas keeping its leg access once the oxygen gas has its own cylinder). Both browser cases fail against the build before this change. The visual suite captures the locked shared cylinder on a route leg on phone, tablet, and desktop. Gases a leg has not set have their own cases: Vitest checks that a leg's first access edit records the listed gases as set; that an edited leg, and never an unedited one, gets one `ROUTE_GAS_ACCESS_UNSET` warning for each gas that joined later (switched back on, a travel gas, a new gas, or the other breathing mode's gases) while the cave input still leaves that gas out; that ticking, unticking, or keeping it not carried clears the warning, and keeping it not carried leaves the cave input byte-identical; that a gas switched off after being set stays set; that gases on a shared cylinder are left to its blocking error; and (`src/app/recalculation.test.ts`) that two chained library recalculations carry the warning into each new revision while the parent record stays unchanged. Playwright covers a deco gas added after a leg was edited: the leg (in a status region) and Setup name it, the calculation is not blocked, Review and the saved snapshot carry the warning, and "Keep not carried" clears it while the result stays current. The visual suite captures that leg notice on phone, tablet, and desktop.

Native sync/build is a separate gate from web tests. Opening, signing, submitting, deploying, or publishing a native artifact is not performed by this repository.

The runtime floor has its own coverage. Vitest (`src/platform/runtimeFloor.test.ts`) loads `vite.config.ts` through Vite and checks that `build.target` and `build.cssTarget` are exactly the documented list, that every `IPHONEOS_DEPLOYMENT_TARGET` in the Xcode project equals that iOS version, that the version is at least 15.4, that the Chrome target is no newer than the last WebView the Android `minSdkVersion` can install (119 for API 24), and that `tsconfig.json` lists no ES library newer than ES2022. Compile-time probes in the same file make `tsc` fail if Node globals, ES2023 array methods, or `DisposableStack` start to type-check in the app program. The desktop smoke suite checks that the built stylesheets load, have no range-syntax media queries (`width>=900px`), which WebKit ignores before 16.4, and still contain every `min-width` and `max-width` breakpoint in `src/styles`; that check fails on the earlier build, which used Vite's default target. No suite runs the bundle in WebKit 15.4 or an old Android WebView.

Tank Bank gases that share an identifier have their own coverage. Vitest (`src/app/planning.test.ts`) resolves a back gas and a pony that are both stored as `air` to `air` and `air~2`, each cylinder's gas snapshot equal to its gas, and calculates that plan (the planner used to reject it with `GAS_ID_DUPLICATE`). It checks that the renamed input is byte-identical to the input from a Tank Bank whose records already had distinct identifiers, and that distinct identifiers, excluded gases, and gas-only plans are not renamed. Two identical EAN50 stages from different records, both breathed, are each charged to their own cylinder, exactly as in that control. A pony in the 21 m deco slot is switch-eligible there but loses the identifier tie to the first-listed back gas, so it takes no gas. One record selected for two gases keeps one identifier and still fails with `GAS_ID_DUPLICATE` on its second gas. An ad hoc identifier is never renamed, even when the record that repeats it is listed first. Ten stages from ten records keep `ean50` and then number `~02` to `~10`, so `compareGasPreference` orders them in plan order, and a hand-edited stored `air~2` becomes `air~2~2` rather than repeating a renamed identifier. A CCR air diluent with an air bailout charges the bailout cylinder, and with dil-out the dedicated bailout still comes first. A Cave lost-back-gas scenario exits on a stage stored with the back gas's identifier. A characterization test pins the remaining limitation: identical mixes whose stored identifiers already differ tie by identifier, not plan order. `src/app/recalculation.test.ts` checks that a saved snapshot keeps the renamed identifier and recalculates unchanged. Playwright plans a Tank Bank cylinder with its Duplicate (bottom gas and a deco gas) and a CCR air diluent with an air bailout added separately; it checks the status, which cylinder the ledger charges, the saved snapshot's identifiers, and, for the Duplicate case, that the stored Tank Bank is unchanged. Both browser cases fail against the unfixed build.

Depth entry and diagnostic presentation have their own coverage. Vitest checks depth entry (`src/app/helpers.test.ts`: whole-foot display, exact re-entry of a displayed depth, shallower-only switch alignment on 3 m and 10 ft grids for deco, bailout, and CCR switches, a retyped off-grid deco switch moving onto the shallower stop it displays as, new entries taken at the displayed precision (switch depths toward the surface), and a property check that every printed MOD re-entered as a deco switch depth is accepted), safe-side limit rounding (`src/domain/units.test.ts`), switch-depth and setpoint diagnostics, including the travel-gas and bottom-gas switch errors, that state the value, the limit, and the depth (`src/domain/validation.test.ts`), diagnostic depths restated in feet, matching only whole printed depths, restating multi-depth warnings saved before depth mentions existed, and never mixing units (`src/app/diagnosticText.test.ts`), between-stop travel folded into the stop rows, keeping the travel gas when a switch on arrival changes it, telling apart gases that share a name, and keeping a legacy CCR loop leg apart from the same diluent breathed open circuit, with an invariant that a calculated plan keeps every second and its order (`src/app/runtimeRows.test.ts`), the hidden bottom-gas switch depth, and Tank Bank source changes counted only for the gases and records the calculation used, so Include toggles and switched-off cylinders do not ask for an update while a record revised before a switch between OC and CCR still does (`src/app/planning.test.ts`, `src/app/workspaceStatus.test.ts`), and a pin on the diagnostic codes the legacy fixtures emit (`src/engine/legacy-golden.test.ts`). Playwright covers the tester-reported cases: oxygen at 20 ft and EAN50 at 70 ft calculating with folded stop rows (the oxygen row naming "EAN50 → Oxygen" and the switch on arrival), a retyped displayed switch depth keeping the plan current, the printed low-setpoint maximum being accepted, Gas Density in g/L, and stored warning depths restated in feet in the Saved Plans library.

## Plan and Cave workflow coverage

Browser coverage includes:

- accessible Setup and Review views for the top-level Plan and Cave workspaces;
- an explicit first Plan or Cave calculation followed by debounced recalculation after direct valid edits;
- immediate suppression of superseded numeric output and Save while either workspace is updating or invalid, followed by restoration only when the result signature matches;
- retention of the current Plan and Cave drafts and matching results while navigating among primary workspaces during the app session;
- Plan calculations and saved snapshots with two Tank Bank cylinders whose gases share an identifier (a cylinder and its Duplicate, and a CCR air diluent with an air bailout);
- explicit Tank Bank source-revision update actions, with no silent replacement of either calculated snapshot;
- a blocking "Cylinder shared" state in Plan while one Tank Bank cylinder is selected for more than one active gas, and source controls in Plan and Cave that name and disable a record another active gas uses;
- a blocking "Source unavailable" state in Plan and Cave for an archived, deleted, quarantined, or unreadable Tank Bank source, which names the reason and the ad hoc detach values and calculates nothing until the diver chooses another cylinder or detaches;
- Cave leg access and stages that stay with each gas when its cylinder source changes, and a blocking "Cylinder shared" state while one Tank Bank cylinder is selected for more than one gas;
- a non-blocking Cave warning, on the leg and in Setup, Review, and the saved snapshot, for a gas that joined the plan after a leg's cylinders were set, cleared by ticking the gas or keeping it not carried;
- an aggregate Cave Review status and diagnostics list that remains unsafe when any enabled, unselected failure scenario contains an error;
- time-accurate runtime/depth axes, color-matched phase and travel-duration summaries, emitted phase/decompression/switch/reserve-crossing visuals, and one directly labeled selected endpoint ceiling checkpoint in primary, CCR bailout, Cave base/scenario, and stored base-plan contexts;
- mouse, touch, and keyboard timeline scrubbing with an accessible docked current-value readout, selectable numbered event details, graph/readout non-overlap, and touch scrolling preserved;
- Settings remaining limited to display and local preferences while exposing distinct app and calculation-engine identifiers; and
- Cave route-leg insertion scrolling and current scenario/result selection behavior.

These workflow cases verify application state and rendering only. They do not establish decompression, gas, reserve, cave-procedure, safety, compatibility, or planner-parity validity.

## Tools redesign coverage

Calculator and application tests cover the eleven production functions and the redesigned workspace. Focused coverage includes:

- category navigation and all eleven named capabilities on phone, tablet, and desktop layouts;
- live recalculation and visible assumptions for simple tools, with per-session inputs not persisted as plans;
- Emergency Gas entered-schedule behavior, optional OC cylinder checks, required single-cylinder CCR checks, Rock Bottom / Minimum Gas and Simplified Bailout mode binding, insufficient-gas diagnostics, debounced live updates, and suppression of superseded results and Plan actions;
- Tank Bank snapshotting, manual detachment, and no silent refresh after a Tank Bank edit;
- exact `Use in Plan` patches and rejection of unrelated-field mutation;
- versioned Best Mix/END narcotic policy, SAC / RMV direct-volume versus cylinder-pressure-drop modes, absolute/gauge/delta pressure semantics, exact MOD Feet/Meters conversion, surface-gas and RMV display-unit changes that preserve canonical values, and context-specific Tank Bank availability.

The browser suite contains functional smoke coverage plus committed visual cases spanning phone, tablet, and desktop. The visual suite captures the Plan and Cave Setup and Review states, including the responsive profile graph, the Tools library, a simple live result, and the full live Emergency Gas workspace without a fixed calculation control competing with primary navigation. Exact passing counts come from the current command output rather than this document.

These cases prove deterministic arithmetic and application behavior only. No test result is independent safety validation or a compatibility/parity claim.

## Proposed release checks

- **Automated build:** Add an assertion for the fixed title, description, PWA icon set, and absence of user data in public metadata. The release process checks those values through built-file and deployed-response inspection until that test exists.
- **Storage states:** Add phone, tablet, and desktop visual baselines for the Tank Bank quarantine notice and unreadable state. The desktop smoke suite asserts them functionally only.
- **Native interaction:** Add phone and tablet profile-scrubber coverage when the project starts a signed mobile release path. Current Playwright touch-pointer coverage exercises the web implementation.
- **Runtime floor:** Before a native release, open the app on an iOS 15.4 simulator or device and on Android 7 (API 24, the `minSdkVersion`) with WebView 111, the oldest Chrome the build targets. Playwright covers current Chromium only.
## Safety gaps

Representative OC multigas, CCR, and CCR-bailout comparison envelopes are committed, including their schedule-distribution differences. Broader multi-depth/mix, altitude, CNS-output, reserve, and cave evidence remains incomplete. Every ascent leg re-checks its arrival ceiling (engine 0.2.1), but the scheduler does not look ahead through a stop. After a switch from a nitrogen-rich loop to a lean, helium-heavy bailout gas, the ceiling can deepen past a held stop without a diagnostic; exploratory comparisons found this by up to 7.0 m. A deepened first stop anchors the gradient-factor line deeper and can shorten a schedule. The arrival re-check has internal invariants and uncommitted exploratory comparisons against engine 0.2.0, not independent reference vectors, and the stuck-stop waypoint arrival check has no dedicated test. Cave ceiling-safe routes require explicit route modeling and qualified cave-diver review. Compatibility presets remain experimental and are not parity tests.
